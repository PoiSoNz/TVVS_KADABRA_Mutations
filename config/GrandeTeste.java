

public class GrandeTeste {
    private static int K = 0;

    /**
     * Edge detection operation that uses three convolution steps:
     *   1. Gaussian
     *   2. Vertical
     *   3. Horizontal
     * A binary image is outputed based on a threshold.
     */
    public static int GrandeTeste(int[][] inputImage) {
        GrandeTeste.K = 3;
        int c = (GrandeTeste.K);
        int d = GrandeTeste.K;
        int e = 5;
        GrandeTeste.K += 1;
        GrandeTeste.K = (GrandeTeste.K);
        GrandeTeste.K = (GrandeTeste.K) | 4;
        Boolean b = (GrandeTeste.K) < 4;
        return GrandeTeste.K;
    }
}

